# Nishant-Thottarath 

# User gives 2 numbers and we find the reminder number1/number 2 
number1 = input('Enter first number: ')
number2 = input('Enter second number: ')

# finds the remainder of the division
remainder = float(number1) % float(number2)

# Display the remainder of two numbers
print('The remainder of dividing number1 by number 2 is ' + str(remainder))
